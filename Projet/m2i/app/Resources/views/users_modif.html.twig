<!doctype html>
<html lang="en">
<head>
	<meta charset="utf-8">
	<title>Modif Users</title>
	<link href="{{ asset('./css/main.css') }}" type="text/css" rel="stylesheet" />
</head>
<body>
	<div align="right"><a href="{{ path('m2iprojet_admin_user_route')}}" title="retour">
	<img src="{{ asset('./images/retour.jpg') }}" width="40px" height="40px" style="border-radius:20px;border: black solid 1px"></a></div> 
	<h3>Modifier cet utilisateur : </h3>
	
	<img src="{{ asset(selected_user.avatar) }}" width="40px" height="40px" style="border-radius:20px;border: black solid 1px"/>
	
	
	{{ form_start(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
	
	<a href="{{ path('m2iprojet_admin_user_route') }}"><button>Annuler</button>
</body>
</html>